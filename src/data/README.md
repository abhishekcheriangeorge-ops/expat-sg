# Legacy TypeScript copies

**Do not edit these for publishing.** The live corpus is [`/data/*.json`](../../data/README.md) on GitHub.

These modules exist only as a generator (`scripts/export-cms.ts`) for the JSON files. The running app never reads them.
